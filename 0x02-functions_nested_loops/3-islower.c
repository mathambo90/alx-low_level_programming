#include "main.h"
/**
 * _islower - check character size
 * @c: int character
 * Description : function that checks for lowercase character
 * Return : always zero
 */
int _islower(int c)
	char = char;
{
	(char = 'a', char <= 'z', char++);
{

if (char == c);
}
	
return (1);

{
else
}
return (0);
}
